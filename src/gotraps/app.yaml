application: go-traps
version: 0-0-1
runtime: go
api_version: go1

handlers:
- url: /static
  static_dir: static
  expiration: "1h"
- url: /assets
  static_dir: static/workaround/assets
  expiration: "1h"
- url: /content/*.json
  static_files: /content/*.json
  upload: /content/*.json
  mime_type: application/json
- url: /content
  static_dir: content
  expiration: "1h"
- url: /
  static_files: index.html
  upload: index\.html
- url: /compile
  script: _go_app